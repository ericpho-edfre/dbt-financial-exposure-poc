version: 2

sources:
  - name: raw
    schema: dbt_epho_raw            # This must match the actual Snowflake schema
    tables:
      - name: project_tracker
        description: "Seeded project tracker data from CSV"